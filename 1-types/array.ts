{
  // Array
  const fruits: string[] = [];
  const scores: Array<number> = [];

  function printArray(fruits: readonly string[]) {
		// readonly 데이터변경이 불가능하다
  }

	// Tuple
	let student: [string, number];
	student = ['name', 123];
}